(take std:io println)

(fn consume ((message String)) -> unit
  (println &message))

(fn main () -> i32
  (let original "owned string")
  (let copy (clone original))
  (consume copy)
  (println &original)
  0)

(test "integer values are Copy"
  (do
    (let value 21)
    (= (+ value value) 42)))
