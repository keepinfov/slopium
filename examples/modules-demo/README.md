# Multi-file v0.2 example

This package demonstrates path-derived modules, explicit exports/import
aliases, a generic function, the bundled standard library, and
`List<String>` ownership.

```sh
slopium --manifest-path examples/modules-demo/Slopium.toml run
slopium --manifest-path examples/modules-demo/Slopium.toml test
```
