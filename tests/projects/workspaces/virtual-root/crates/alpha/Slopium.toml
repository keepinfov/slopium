[package]
name = "alpha"
version.workspace = true
source = "src"
entry = "src/main.slp"

[dependencies]
std = { toolchain = true }
beta = { path = "../beta" }

[build]
target = "x86_64-unknown-linux-gnu"
