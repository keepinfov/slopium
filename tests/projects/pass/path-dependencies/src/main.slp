(take std:io println-i64)

(take mathlib:lib answer)

(fn main () -> i32
  (println-i64 (answer))
  (println-i64 (foundation:numbers:forty))
  0)

(test "direct path dependency"
  (= (answer) 42))

(test "transitive dependency namespace"
  (= (foundation:numbers:forty) 40))
