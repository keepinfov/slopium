[package]
name = "module-tests"
version = "0.15.1"
source = "src"
entry = "src/main.slp"

[dependencies]
std = { toolchain = true }

[build]
target = "x86_64-unknown-linux-gnu"

[profile.release]
opt-level = 1
debug = false
