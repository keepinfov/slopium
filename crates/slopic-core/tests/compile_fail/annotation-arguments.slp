; `inline` is a hint and carries nothing. An argument here would be a second
; spelling of the same annotation, which is what the table exists to prevent.
(fn (inline "always") hot ((x i64)) -> i64 (* x x))

(fn main () -> i32 0)
