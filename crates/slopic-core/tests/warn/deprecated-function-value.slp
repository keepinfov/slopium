; Taking a function as a value is a use of it, so it warns where a call does.
(fn (deprecated "there is a combinator for this") twice ((x i64)) -> i64 (* x 2))

(fn apply ((f (Fn (i64) i64)) (x i64)) -> i64 (f x))

(fn main () -> i64
  (apply twice 21))
