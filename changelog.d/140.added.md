- Added `bench/`: seven kernels in Slopium, C, Rust, Python and Java with a
  harness that builds, verifies and measures them (`nix develop .#bench -c
  python3 bench/run.py`), plus `devShells.bench` for the toolchains.
